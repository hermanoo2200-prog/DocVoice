<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema escuro fixo: leitura longa. Sem DayNight — não há versão clara. -->
    <style name="Theme.DocVoice" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/fundo</item>
        <item name="android:statusBarColor">@color/fundo</item>
        <item name="android:navigationBarColor">@color/fundo</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
